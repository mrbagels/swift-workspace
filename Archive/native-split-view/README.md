# Native Split View Archive

This folder is a non-compiling reference archive for the removed native split
view Mac shell implementation.

The active `MacWorkspaceShell` product is custom-only. These `.swift.reference`
files are not included by SwiftPM, XcodeGen, demo targets, or starter apps. Keep
them here only as historical reference while the custom shell remains the sole
supported Mac renderer.
